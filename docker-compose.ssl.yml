# Usage: docker compose -f docker-compose.infra.yml -f docker-compose.ssl.yml up -d --build
services:
  gateway:
    volumes:
      - ./certs:/etc/nginx/certs:ro
      - ./apps/gateway/nginx.ssl.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "443:443"
      - "8443:8443"
