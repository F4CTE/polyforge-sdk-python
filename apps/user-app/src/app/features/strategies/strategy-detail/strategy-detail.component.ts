import { Component } from '@angular/core';

@Component({ selector: 'app-strategy-detail', standalone: true,
  template: `<p class="text-muted">Strategy detail — coming soon.</p>` })
export class StrategyDetailComponent {}
