{
  "name": "@polyforge/crypto",
  "version": "0.1.0",
  "private": true,
  "main": "./pkg/polyforge_crypto.js",
  "types": "./pkg/polyforge_crypto.d.ts",
  "exports": {
    ".": {
      "import": "./pkg/polyforge_crypto.js",
      "require": "./pkg/polyforge_crypto.js",
      "types": "./pkg/polyforge_crypto.d.ts"
    }
  },
  "scripts": {
    "build:wasm": "if [ \"$CI\" = \"true\" ]; then echo 'CI: using pre-built WASM artifacts'; else bash build.sh; fi",
    "build": "if [ \"$CI\" = \"true\" ]; then echo 'CI: using pre-built WASM artifacts'; else bash build.sh; fi"
  }
}
