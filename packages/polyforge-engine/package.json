{
  "name": "@polyforge/engine",
  "version": "0.1.0",
  "private": true,
  "main": "./pkg/polyforge_engine.js",
  "types": "./pkg/polyforge_engine.d.ts",
  "exports": {
    ".": {
      "import": "./pkg/polyforge_engine.js",
      "require": "./pkg/polyforge_engine.js",
      "types": "./pkg/polyforge_engine.d.ts"
    }
  },
  "scripts": {
    "build:wasm": "if [ \"$CI\" = \"true\" ]; then echo 'CI: using pre-built WASM artifacts'; else bash build.sh; fi",
    "build": "if [ \"$CI\" = \"true\" ]; then echo 'CI: using pre-built WASM artifacts'; else bash build.sh; fi"
  }
}
