{
  "name": "@polyforge/crypto-native",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "crypto-native",
    "triples": {}
  },
  "scripts": {
    "build": "node -e \"try{require('child_process').execFileSync('cargo',['--version'],{stdio:'pipe'})}catch{console.log('Warning: cargo not available, using pre-built .node binary');process.exit(0)}\" && napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  }
}
