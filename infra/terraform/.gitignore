# Terraform state (stored in S3 — never commit locally)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Secret variable files
*.tfvars
!terraform.tfvars.example

# Crash log files
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
