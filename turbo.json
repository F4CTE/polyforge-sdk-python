{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalDependencies": ["tsconfig.json"],
  "tasks": {
    "generate": {
      "cache": false,
      "inputs": ["prisma/**"]
    },
    "build": {
      "dependsOn": ["^build", "build:wasm", "generate"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**"]
    },
    "build:wasm": {
      "inputs": ["$TURBO_DEFAULT$", "!target/**"],
      "outputs": ["pkg/**", "*.node"]
    },
    "build:swagger": {
      "dependsOn": ["^build"],
      "inputs": ["src/**"],
      "outputs": ["dist/swagger.json", "dist/swagger-admin.json"],
      "cache": false
    },
    "generate:api": {
      "dependsOn": ["build:swagger"],
      "inputs": [
        "dist/swagger.json",
        "../../openapi-ts.config.ts",
        "../../openapi-ts.admin.config.ts"
      ],
      "outputs": ["src/app/api/**"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/eslint.config.mjs"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "inputs": ["$TURBO_DEFAULT$", "**/*.d.ts", "$TURBO_ROOT$/tsconfig.json"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**"],
      "outputs": ["coverage/**"]
    },
    "test:cov": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**"],
      "outputs": ["coverage/**"]
    }
  }
}
